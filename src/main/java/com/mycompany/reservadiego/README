Hay un archivo en la carpeta Setup que crea la base de datos crea las tablas y inserta los valores por defecto en ella por si se desea usar este proyecto

En el archivo de conexion de la carpeta coenxion cambiar los tres final static para que concuerden con tu base de datos